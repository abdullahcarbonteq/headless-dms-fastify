//#region src/tuple.d.ts
declare const TUPLE_KEY: unique symbol;
interface Tuple<Inner> extends Array<Inner> {
  [TUPLE_KEY]: boolean;
}
declare const setAsTuple: <Inner, T extends Array<Inner>>(arr: T) => Tuple<Inner>;
declare const isTuple: <Inner>(t: unknown) => t is Tuple<Inner>;
type AppendToTuple<T, U> = T extends [...infer Rest, infer L] ? [...Rest, L, U] : [T, U];

//#endregion
export { AppendToTuple, Tuple, isTuple, setAsTuple };
//# sourceMappingURL=tuple.d.cts.map